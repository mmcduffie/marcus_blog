<% @posts.each do |post| %>
  <div id="post_title"><%= post.title %></div>
  <div id="post_timestamp"><%= post.created_at.in_time_zone('America/New_York').to_formatted_s(:long_ordinal) %></div>
	<div id="post_body"><%= post.body %></div>
	<div id="post_footer"></div>
<% end %>